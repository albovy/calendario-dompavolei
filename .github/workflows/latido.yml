# Prueba temporal: comprobar si GitHub lanza ejecuciones programadas en este repositorio.
name: Prueba de programación
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  latido:
    runs-on: ubuntu-latest
    steps:
      - run: echo "La programación funciona - $(date -u)"
